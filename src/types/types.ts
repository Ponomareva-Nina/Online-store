export type NullableElement<T> = T | null;

export enum PossibleUrlParams {
    ID = 'id',
    CATEGORY = 'category',
    FACULTY = 'faculty',
    SORT = 'sort',
    PRICE = 'price',
    STOCK = 'stock',
}
